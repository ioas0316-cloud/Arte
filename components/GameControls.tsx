import React from 'react';

// This component is currently unused but is being kept to resolve a build error.
// An empty file can cause an "Unexpected end of input" syntax error during parsing.
const GameControls: React.FC = () => {
  return null;
};

export default GameControls;
